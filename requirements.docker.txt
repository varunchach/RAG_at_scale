# Runtime-only requirements for the Docker service image.
# Excludes PySpark, pytest, awscli, pyarrow — not needed at serve time.

# CPU-only torch — 250 MB vs 1.5 GB GPU build
--extra-index-url https://download.pytorch.org/whl/cpu
torch

# Embeddings & search
sentence-transformers
faiss-cpu
rank-bm25
onnxruntime

# API
fastapi
uvicorn[standard]
aiofiles
python-multipart

# AWS
boto3

# Observability
python-json-logger
opentelemetry-api
opentelemetry-sdk
opentelemetry-exporter-otlp
opentelemetry-instrumentation-fastapi
prometheus-client

# Config & utils
pydantic
pydantic-settings
python-dotenv
requests
httpx
