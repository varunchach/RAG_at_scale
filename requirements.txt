# Core Data Processing
pyspark==3.5.0
pandas
numpy

# ML & Embeddings — CPU-only torch keeps the Docker image ~250 MB instead of ~2 GB
--extra-index-url https://download.pytorch.org/whl/cpu
torch
sentence-transformers
onnxruntime
transformers

# Vector & Search
faiss-cpu            # in-memory dense search (swap for faiss-gpu on GPU nodes)
rank-bm25
pyarrow              # required for PySpark Pandas UDFs

# Reranking
# cohere             # optional: cloud reranker

# Service & Observability
fastapi
uvicorn[standard]
httpx                # used by FastAPI TestClient
python-json-logger
opentelemetry-api
opentelemetry-sdk
opentelemetry-exporter-otlp
opentelemetry-instrumentation-fastapi
prometheus-client
pydantic
pydantic-settings    # required: BaseSettings moved out of pydantic v2 core
python-dotenv
requests

# Testing
pytest
pytest-cov

# AWS
boto3
awscli

# Utilities
python-multipart
aiofiles
